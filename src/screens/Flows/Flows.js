import React from 'react';
import { View, Text } from 'react-native';

import ImageGenerationContainer from '../../components/ImageGeneration/ImageGenerationContainer';

const Flows = () => {
  return <ImageGenerationContainer />;
};

export default Flows;
