export const EMPTY_ARRAY = Object.freeze([]);
export const EMPTY_OBJECT = Object.freeze({});
export const EMPTY_FUNCTION = Object.freeze(() => {});
export const EMPTY_STRING = Object.freeze('');

export const LOGO_IMAGE_SIZE = 320;
